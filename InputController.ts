namespace ChessGame {
    import f = FudgeCore;
    export class InputController{
        private _places: f.Node[];
        constructor(places: f.Node[],) {
            console.log("");
            this._places = places;

        }
    }
}